export default function DashboardPage() {
  return (
    <div className="space-y-4">
      <h1 className="text-2xl font-bold">Dashboard</h1>
      <p className="text-sm text-gray-600">
        This is a placeholder dashboard. Routing and auth are wired up. Next
        blocks can hook this into the real API (applications, pipeline, etc.).
      </p>
    </div>
  );
}
