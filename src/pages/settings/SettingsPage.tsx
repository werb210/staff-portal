import Card from "@/components/ui/Card";

const SettingsPage = () => (
  <div className="page">
    <Card title="Settings">
      <p>Configure your preferences, notification channels, and integrations.</p>
    </Card>
  </div>
);

export default SettingsPage;
