# Base URL for the Staff API. Must include protocol and host. Do not include a trailing slash.
VITE_API_BASE_URL=https://api.staff.boreal.financial
VITE_JWT_STORAGE_KEY=portal_auth_token

# Twilio dialer mode (mock prevents outbound calls in development)
VITE_TWILIO_MODE=mock
