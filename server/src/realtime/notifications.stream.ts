// placeholder for future websocket integration
export function pushNotification(userId: string, payload: any) {
  // Codex will later replace this with WS broadcast logic
  console.log("Realtime notification queued:", userId, payload);
}
